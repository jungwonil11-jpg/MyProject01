# 2026-05-16 GuestBookVO — 67% (단일 파일)

## 회차 정보
- **파일**: GuestBookVO
- **난이도**: 67%
- **모드**: 단일 파일 (PROGRESS.md 갱신 X)

## 빈칸 카테고리

| 빈칸 자리 | 개수 |
|---|---|
| Lombok 어노테이션 (`@Data`, `@AllArgsConstructor`, `@NoArgsConstructor`) | 3 |
| 필드 타입 (`String`, `MultipartFile`) | 2 |
| **합계** | **5** |

## 살린 것 (67% 기준)
- `private` 접근제어자
- 필드명 (`g_idx`, `g_writer`, ...)
- 패키지/import 구문

## 진행 방법
1. IDE에서 `GuestBookVO.java` 열기
2. `_____` 5개 채우기
3. 끝나면 `/연습 채점`

## 힌트
- `@Data` 류 어노테이션 3개의 역할: getter/setter 자동 생성, 인자 다 있는 생성자, 인자 없는 생성자
- 필드 타입 2개: 텍스트 데이터용 / 파일 업로드용

막히면 `/힌트`.
