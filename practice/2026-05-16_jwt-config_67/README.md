# 연습 회차 — JwtConfig 67%

## 회차 정보
- **파일**: JwtConfig.java (단일 파일 모드)
- **난이도**: 67%

## 빈칸 카테고리 (P1~P7)
- **P4 어노테이션 + 파라미터**: `@Value("${...}")` × 3, `@Bean`
- **P7 변수 타입 / 시그니처 리턴 타입**: `String`, `long`, `JwtUtil` (리턴 타입)

## 학습 포인트
1. `@Configuration` — 이 클래스가 Spring 설정 클래스임을 알림
2. `@Value("${...}")` — `application.yaml`의 값을 필드에 주입
3. `@Bean` — 메소드 리턴 객체를 Spring 컨테이너에 등록 (싱글톤)

## 진행
IDE 열고 `_____` 채워. 끝나면 `/연습 채점`.
